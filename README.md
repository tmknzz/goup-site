# GoUp — support site

Public pages required by the App Store for the macOS Safari extension **GoUp**.
The application source lives in a separate private repository.

- `index.html` — support page (Support URL)
- `privacy.html` — privacy policy (Privacy Policy URL)

Both pages are bilingual: Japanese first, English below the divider.
